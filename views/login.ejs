<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="icon" type="images/x-icon" href="/img/isotipo con fondo.png"/>

    <!-- Bootstrap -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL" crossorigin="anonymous"></script>

    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
    
    <!-- Propietary CSS -->
    <link rel="stylesheet" href="/stylesheets/main.css">
    <link rel="stylesheet" href="/stylesheets/login.css">
</head>
<body>
    <form action="/login" method="post" class="position-absolute top-45 start-50 translate-middle">
        <div class="d-flex">
            <img src="/images/logotipo-1.png" class="mx-auto mt-5 mb-4 img-fluid" alt="Ivory Homes">
        </div>
        <p class="h2 text-center">Iniciar Sesión</p>
        <div class="wrap-input my-3">
            <label for="email" class="form-label"><span class="fa fa-user fa-form me-2 mb-0"></span>Usuario:</label>
            <input type="email" class="form-control" placeholder="ivory@homes.es" name="email">
        </div>
        <div>
            <label for="pwd" class="form-label"><span class="fa fa-lock fa-form me-2 mb-0"></span>Contraseña:</label>
            <input type="password" class="form-control" id="pwd" placeholder="********" name="pass">
        </div>

        <div class="d-flex justify-content-center mt-4">
            <button type="submit" class="btn px-60">Acceder</button>
        </div>
        <div class="d-flex justify-content-center mt-4">
            <a href="/login/recovery">¿Olvidó su contraseña?</a>
        </div>

        <% if (message == "missing") { %>
            <div class="alert alert-warning mt-3" role="alert">
                Por favor rellene todos los campos!
            </div>
        <% } else if (message == "false") { %>
            <div class="alert alert-danger mt-3" role="alert">
                Usuario y/o contraseña incorrectos!
            </div>
        <% } else if (message == "success") { %>
            <div class="alert alert-success mt-3" role="alert">
                Contraseña actualizada!
            </div>
        <% } else if (message == "auth") { %>
            <div class="alert alert-warning mt-3" role="alert">
                Debe iniciar sesión para poder acceder a la página!
            </div>
        <% } %>
    </form>
</body>
</html>